# 快速开始指南

## 5分钟快速上手

### 步骤1: 检查环境

```bash
python check_env.py
```

如果所有检查都通过，继续下一步。如果有错误，按照提示解决。

### 步骤2: 安装依赖（如果需要）

```bash
pip install fastapi uvicorn openai python-dotenv aiofiles
```

### 步骤3: 设置API Key

**方式1: 使用配置向导 (最简单) ⭐**

```bash
python setup_config.py
# 按提示输入API Key即可
```

**方式2: 手动创建配置文件**

```bash
# 复制示例配置
copy config.example.json config.json

# 编辑config.json，填入你的API Key
# {
#   "openai_api_key": "sk-your-api-key-here"
# }
```

**方式3: 使用环境变量**

```bash
# Windows PowerShell
$env:OPENAI_API_KEY="sk-xxx"

# Windows CMD
set OPENAI_API_KEY=sk-xxx

# Linux/Mac
export OPENAI_API_KEY="sk-xxx"
```

### 步骤4: 处理第一个task

```bash
python process_single.py task_0001
```

等待处理完成（通常需要10-30秒）。

### 步骤5: 运行生成的项目

```bash
cd task_0001/output
npm install
npm run dev
```

打开浏览器访问 `http://localhost:5173`

## 完整示例

```bash
# 1. 检查环境
python check_env.py

# 2. 设置API Key (选择一种方式)
# 方式1: 配置向导 (推荐)
python setup_config.py

# 方式2: 手动编辑config.json
copy config.example.json config.json
# 然后编辑config.json填入API Key

# 方式3: 环境变量
$env:OPENAI_API_KEY="sk-xxx"

# 3. 处理task
python process_single.py task_0001

# 4. 进入output目录
cd task_0001/output

# 5. 安装依赖
npm install

# 6. 启动开发服务器
npm run dev

# 7. 浏览器访问
# http://localhost:5173
```

## 批量处理多个tasks

```bash
# 处理所有tasks
python process_task.py

# 或者逐个处理
python process_single.py task_0001
python process_single.py task_0002
python process_single.py task_0003
```

## 使用API服务

如果你想通过API接口调用：

```bash
# 1. 启动后端服务
cd screenshot-to-code/backend
python main.py

# 2. 在另一个终端调用API
curl -X POST http://localhost:8000/api/batch-process \
  -H "Content-Type: application/json" \
  -d "{\"task_dir\": \"D:/path/to/task_0001\", \"api_key\": \"sk-xxx\"}"
```

## 常见问题

### 问题1: 找不到模块

```bash
# 解决方法：安装依赖
pip install fastapi uvicorn openai python-dotenv aiofiles
```

### 问题2: API Key错误

```bash
# 方式1: 检查config.json文件
# 确保格式正确，API Key以sk-开头

# 方式2: 检查环境变量
echo $env:OPENAI_API_KEY  # Windows PowerShell
echo %OPENAI_API_KEY%     # Windows CMD
echo $OPENAI_API_KEY      # Linux/Mac

# 优先级: config.json > 环境变量
```

### 问题3: 生成的项目无法运行

```bash
# 确保Node.js版本 >= 18
node --version

# 重新安装依赖
cd task_0001/output
rm -rf node_modules package-lock.json
npm install
npm run dev
```

## 目录结构说明

```
task_0001/
├── checkpoints/          # 输入：截图文件
│   ├── step_00.png
│   ├── step_01.png
│   └── ...
├── assets/              # 输入：静态资源
│   ├── css/
│   ├── images/
│   └── ...
├── output/              # 输出：生成的Vue3项目 ⭐
│   ├── index.html
│   ├── package.json
│   ├── vite.config.ts
│   └── src/
│       ├── main.ts
│       ├── App.vue
│       └── assets/      # 自动复制的资源
└── ground_truth/        # 参考：标准答案
    └── index.html
```

## 下一步

1. ✅ 运行 `python check_env.py` 检查环境
2. ✅ 运行 `python process_single.py task_0001` 处理第一个task
3. ✅ 查看生成的 `task_0001/output/` 目录
4. ✅ 运行生成的Vue3项目
5. 📖 阅读 `使用说明.md` 了解更多功能
6. 📖 阅读 `二次开发说明.md` 了解技术细节

## 获取帮助

- 查看 `使用说明.md` - 详细使用文档
- 查看 `二次开发说明.md` - 技术实现细节
- 检查 `screenshot-to-code/backend/routes/batch_process.py` - 核心处理逻辑
- 检查 `screenshot-to-code/backend/codegen/vue_extractor.py` - 文件提取逻辑
