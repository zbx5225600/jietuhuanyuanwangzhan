# API Key配置方式总结

## 三种配置方式对比

| 方式 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **配置向导** | ✅ 最简单<br>✅ 交互式<br>✅ 自动验证 | 需要手动运行 | 首次配置 |
| **配置文件** | ✅ 持久化<br>✅ 可配置多项<br>✅ 无需重复设置 | 需要手动编辑 | 开发环境 |
| **环境变量** | ✅ 灵活<br>✅ 临时使用 | ❌ 重启失效<br>❌ 每次都要设置 | 临时测试 |

## 推荐使用流程

### 首次使用 (推荐)

```bash
# 1. 运行配置向导
python setup_config.py

# 按提示输入:
# - OpenAI API Key
# - 选择AI模型

# 2. 检查配置
python check_env.py

# 3. 开始使用
python process_single.py task_0001
```

### 日常使用

配置一次后，直接使用：

```bash
python process_single.py task_0001
```

### 临时测试不同API Key

```bash
# 使用环境变量临时覆盖
$env:OPENAI_API_KEY="sk-test-key"
python process_single.py task_0001
```

## 配置优先级

```
环境变量 OPENAI_API_KEY
    ↓ (如果未设置)
config.json 中的 openai_api_key
    ↓ (如果都未设置)
报错提示
```

实际上是：**config.json > 环境变量**

如果 `config.json` 存在且有效，会优先使用配置文件。

## 详细步骤

### 方式1: 配置向导 ⭐

```bash
python setup_config.py
```

交互式提示：

```
截图还原工具 - 配置向导
============================================================

请输入你的OpenAI API Key:
(可以从 https://platform.openai.com/api-keys 获取)
API Key (sk-...): sk-proj-xxxxx

选择AI模型:
  1. gpt-4-vision-preview (推荐，最准确)
  2. gpt-4o (更快)
  3. gpt-4-turbo (平衡)
选择 (1-3) [1]: 1

============================================================
✅ 配置文件创建成功!
============================================================
文件位置: D:\workspace\config.json
API Key: sk-proj-xx...
模型: gpt-4-vision-preview
```

### 方式2: 手动配置文件

```bash
# 1. 复制示例
copy config.example.json config.json

# 2. 编辑config.json
notepad config.json

# 3. 填入内容
{
  "openai_api_key": "sk-your-actual-key",
  "model": "gpt-4-vision-preview"
}
```

### 方式3: 环境变量

**Windows PowerShell:**
```powershell
$env:OPENAI_API_KEY="sk-your-key"
```

**Windows CMD:**
```cmd
set OPENAI_API_KEY=sk-your-key
```

**Linux/Mac:**
```bash
export OPENAI_API_KEY="sk-your-key"
```

## 配置文件完整示例

### 最小配置

```json
{
  "openai_api_key": "sk-proj-xxxxx"
}
```

### 完整配置

```json
{
  "openai_api_key": "sk-proj-xxxxx",
  "model": "gpt-4-vision-preview",
  "max_tokens": 4096,
  "temperature": 0.2,
  "openai_base_url": "",
  "debug_enabled": false
}
```

## 验证配置

```bash
python check_env.py
```

输出示例：

```
API Key检查:
  ✅ config.json中已设置: sk-proj-xx...
```

或

```
API Key检查:
  ✅ 环境变量中已设置: sk-xxx...
```

## 安全提示

### ✅ 安全做法

1. 使用 `config.json` (已在 .gitignore 中)
2. 不要提交 `config.json` 到Git
3. 使用 `config.example.json` 作为模板
4. 定期轮换API Key

### ❌ 不安全做法

1. 把API Key写在代码里
2. 提交 `config.json` 到Git仓库
3. 在公开场合分享配置文件
4. 使用明文存储在不安全的位置

## 文件说明

| 文件 | 说明 | 是否提交Git |
|------|------|-------------|
| `config.json` | 实际配置文件，包含真实API Key | ❌ 不提交 |
| `config.example.json` | 配置模板，不包含真实Key | ✅ 提交 |
| `.env` | 环境变量文件 | ❌ 不提交 |
| `.env.example` | 环境变量模板 | ✅ 提交 |

## 常见问题

### Q: 配置文件在哪里？

A: 项目根目录的 `config.json`

### Q: 如何修改配置？

A: 
```bash
# 方式1: 重新运行配置向导
python setup_config.py

# 方式2: 直接编辑
notepad config.json
```

### Q: 配置文件丢失了怎么办？

A: 重新运行配置向导或复制示例文件

### Q: 可以配置多个API Key吗？

A: 目前只支持一个，如需切换可以：
- 修改 `config.json`
- 或使用环境变量临时覆盖

### Q: 如何知道用的是哪个配置？

A: 运行脚本时会显示：
```
API Key: sk-proj-xx...
Model: gpt-4-vision-preview
```

## 快速参考

```bash
# 配置
python setup_config.py              # 配置向导
python check_env.py                 # 检查配置

# 使用
python process_single.py task_0001  # 处理单个
python process_task.py              # 批量处理

# 查看配置
cat config.json                     # Linux/Mac
type config.json                    # Windows CMD
Get-Content config.json             # Windows PowerShell
```

## 相关文档

- [配置说明.md](配置说明.md) - 详细配置文档
- [快速开始.md](快速开始.md) - 快速上手指南
- [使用说明.md](使用说明.md) - 完整使用文档
