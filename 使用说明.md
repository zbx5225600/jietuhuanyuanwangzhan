# 截图还原工具 - 使用说明

## 项目简介

基于screenshot-to-code开源项目二次开发，实现从用户操作截图自动还原成Vue3+Vite项目的功能。

## 功能特点

✅ 从截图自动生成Vue3+Vite项目  
✅ 支持单个task处理和批量处理  
✅ 自动复制静态资源文件  
✅ 生成完整可运行的项目结构  

## 快速开始

### 1. 环境准备

需要安装：
- Python 3.10+
- Node.js 18+
- OpenAI API Key (需要GPT-4 Vision权限)

### 2. 安装依赖

```bash
# 进入backend目录
cd screenshot-to-code/backend

# 安装Python依赖
pip install fastapi uvicorn openai python-dotenv aiofiles

# 或使用poetry
poetry install
```

### 3. 配置API Key

**方式1: 配置文件 (推荐)**

```bash
# 复制示例配置
copy config.example.json config.json

# 编辑config.json
{
  "openai_api_key": "sk-your-api-key-here",
  "model": "gpt-4-vision-preview"
}
```

**方式2: 环境变量**

```bash
# Windows (PowerShell)
$env:OPENAI_API_KEY="your-api-key-here"

# Windows (CMD)
set OPENAI_API_KEY=your-api-key-here

# Linux/Mac
export OPENAI_API_KEY="your-api-key-here"
```

详细配置说明请查看 [配置说明.md](配置说明.md)

## 使用方法

### 方法1: 处理单个task (推荐)

```bash
# 处理task_0001
python process_single.py task_0001

# 处理其他task
python process_single.py task_0002
```

### 方法2: 批量处理所有tasks

```bash
python process_task.py
```

### 方法3: 启动API服务

```bash
cd screenshot-to-code/backend
python main.py
```

然后使用API接口：

```bash
# 列出所有tasks
curl http://localhost:8000/api/tasks

# 处理单个task
curl -X POST http://localhost:8000/api/batch-process \
  -H "Content-Type: application/json" \
  -d "{\"task_dir\": \"D:/path/to/task_0001\", \"api_key\": \"your-key\"}"
```

## 输出结果

处理完成后，生成的Vue3项目会保存在 `task_XXXX/output/` 目录：

```
task_0001/output/
├── index.html              # 入口HTML
├── package.json            # 项目配置
├── vite.config.ts          # Vite配置
├── tsconfig.json           # TypeScript配置
├── tsconfig.node.json      # Node TypeScript配置
└── src/
    ├── main.ts             # 入口文件
    ├── App.vue             # 主组件
    └── assets/             # 静态资源(自动复制)
        ├── css/
        ├── images/
        └── ...
```

## 运行生成的项目

```bash
# 进入output目录
cd task_0001/output

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问 http://localhost:5173
```

## 项目结构说明

```
jietuhuanyuanwangzhan/
├── screenshot-to-code/              # 开源项目(已改造)
│   └── backend/
│       ├── routes/
│       │   └── batch_process.py     # ✨ 批量处理API
│       └── codegen/
│           └── vue_extractor.py     # ✨ Vue文件提取器
├── task_0001/                       # 示例task
│   ├── checkpoints/                 # 截图文件
│   │   ├── step_00.png
│   │   ├── step_01.png
│   │   └── ...
│   ├── assets/                      # 静态资源
│   ├── output/                      # ✨ 生成的Vue3项目
│   └── ground_truth/                # 参考答案
├── process_single.py                # ✨ 单任务处理脚本
├── process_task.py                  # ✨ 批量处理脚本
└── 使用说明.md                      # 本文档
```

## 处理流程

1. 读取task目录中的最后一张截图 (`checkpoints/step_XX.png`)
2. 将截图转换为base64编码
3. 构建专门的Vue3 Prompt
4. 调用OpenAI GPT-4 Vision API生成代码
5. 解析AI响应，提取多个文件
6. 复制assets资源文件到output/src/assets
7. 写入所有文件到output目录

## 配置选项

### 修改AI模型

在 `process_single.py` 或 `process_task.py` 中修改：

```python
request = TaskProcessRequest(
    task_dir=str(task_dir),
    api_key=api_key,
    model="gpt-4-vision-preview"  # 可改为 gpt-4o, gpt-4-turbo
)
```

### 自定义Prompt

修改 `screenshot-to-code/backend/routes/batch_process.py` 中的 `build_vue3_prompt()` 函数。

## 常见问题

### Q: API调用失败

A: 检查：
1. API Key是否正确
2. 是否有GPT-4 Vision权限
3. 账户余额是否充足
4. 网络连接是否正常

### Q: 生成的项目无法运行

A: 确保：
1. 已执行 `npm install`
2. Node.js版本 >= 18
3. 检查控制台错误信息

### Q: 如何对比生成结果

A: 
- 查看 `task_XXXX/ground_truth/` 目录（参考答案）
- 对比 `task_XXXX/output/` 目录（生成结果）
- 使用浏览器同时打开两个版本对比

### Q: 处理速度慢

A: 
- GPT-4 Vision API调用需要时间（通常10-30秒）
- 可以使用批量处理脚本并行处理多个tasks
- 考虑使用更快的模型（如gpt-4o）

## 技术栈

- **后端**: Python 3.10+, FastAPI, OpenAI API
- **生成目标**: Vue 3.4+, Vite 5.0+, TypeScript 5.2+
- **AI模型**: GPT-4 Vision Preview

## 示例输出

处理task_0001后的输出示例：

```
✅ Success!
Output directory: D:/workspace/task_0001/output

Generated files (8):
  ✓ index.html
  ✓ package.json
  ✓ vite.config.ts
  ✓ tsconfig.json
  ✓ tsconfig.node.json
  ✓ src/main.ts
  ✓ src/App.vue
  ✓ src/components/Header.vue

To run the generated project:
  cd D:/workspace/task_0001/output
  npm install
  npm run dev
```

## 下一步

1. 运行 `python process_single.py task_0001` 测试功能
2. 查看生成的output目录
3. 运行生成的Vue3项目
4. 对比ground_truth和output的差异
5. 根据需要调整Prompt模板

## 许可证

基于原项目 [screenshot-to-code](https://github.com/abi/screenshot-to-code) 的许可证。
