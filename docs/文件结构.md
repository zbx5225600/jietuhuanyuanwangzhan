# 文件结构说明

## 根目录保留文件

- `README.md`：总入口
- `process_single.py`：单任务处理
- `process_task.py`：批处理
- `check_env.py`：环境检查
- `setup_config.py` / `setup_config_multi.py`：配置向导
- `config.example.json` / `.env.example`：配置模板

## 目录说明

- `docs/`：合并后的文档
- `tests/`：项目测试脚本
- `screenshot-to-code/`：主代码仓（后端、前端、测试）
- `task_0001/`：示例任务输入与输出目录

## 本次整理原则

- 测试文件统一归档到 `tests/`
- 文档统一归档到 `docs/`
- 删除明显重复文档和无用缓存目录
- 保留可执行脚本与示例数据，避免误删业务必需文件
