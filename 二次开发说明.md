# 截图还原工具 - 二次开发说明

## 项目概述

基于 [screenshot-to-code](https://github.com/abi/screenshot-to-code) 开源项目进行二次开发,实现从用户操作截图自动还原成 **Vue3 + Vite** 项目的功能。

## 功能特性

✅ **截图还原**: 从用户操作截图(checkpoints)自动生成可运行的Vue3项目  
✅ **多文件生成**: 自动提取并生成完整的Vue3+Vite项目结构  
✅ **资源处理**: 自动复制和处理静态资源文件  
✅ **批量处理**: 支持批量处理多个task目录  
✅ **API接口**: 提供RESTful API供前端调用  

## 项目结构

```
jietuhuanyuanwangzhan/
├── screenshot-to-code/          # 开源项目(已改造)
│   ├── backend/                 # Python/FastAPI后端
│   │   ├── routes/
│   │   │   ├── batch_process.py # ✨ 新增:批量处理路由
│   │   │   └── ...
│   │   ├── codegen/
│   │   │   ├── vue_extractor.py # ✨ 新增:Vue多文件提取器
│   │   │   └── ...
│   │   └── prompts/             # Prompt模板(已修改支持Vue3)
│   └── frontend/                # React前端
│       └── src/components/
│           └── batch/
│               └── BatchProcess.tsx # ✨ 新增:批量处理UI
├── task_0001/                   # 示例task目录
│   ├── checkpoints/             # 用户操作截图
│   ├── assets/                  # 资源文件
│   ├── output/                  # ✨ 生成的Vue3项目
│   └── ground_truth/            # 参考答案
└── test_batch_process.py        # ✨ 测试脚本
```

## 快速开始

### 1. 环境准备

```bash
# 进入backend目录
cd screenshot-to-code/backend

# 安装依赖(使用poetry)
poetry install

# 或者使用pip
pip install fastapi uvicorn openai python-dotenv
```

### 2. 配置API Key

```bash
# 设置OpenAI API Key
export OPENAI_API_KEY="your-api-key-here"

# 或者在backend/.env文件中添加
echo "OPENAI_API_KEY=your-api-key-here" > .env
```

### 3. 启动后端服务

```bash
cd screenshot-to-code/backend

# 启动服务
python main.py

# 或使用uvicorn
uvicorn main:app --reload --port 8000
```

服务启动后访问: `http://localhost:8000`

### 4. 测试功能

#### 方式1: 使用测试脚本

```bash
# 在项目根目录运行
python test_batch_process.py
```

#### 方式2: 使用API接口

```bash
# 列出所有tasks
curl http://localhost:8000/api/tasks

# 处理单个task
curl -X POST http://localhost:8000/api/batch-process \
  -H "Content-Type: application/json" \
  -d '{
    "task_dir": "/path/to/task_0001",
    "api_key": "your-openai-api-key"
  }'
```

#### 方式3: 使用前端界面

```bash
cd screenshot-to-code/frontend
npm install
npm run dev
```

访问前端界面,导航到批量处理页面。

## 使用示例

### 处理task_0001

```python
import requests

response = requests.post(
    "http://localhost:8000/api/batch-process",
    json={
        "task_dir": "d:/self-workspace/jietuhuanyuanwangzhan/task_0001",
        "api_key": "sk-xxx"
    }
)

result = response.json()
print(result)
```

生成的项目会保存在 `task_0001/output/` 目录:

```
task_0001/output/
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
├── tsconfig.node.json
└── src/
    ├── main.ts
    ├── App.vue
    └── assets/          # 从task_0001/assets复制
```

### 运行生成的项目

```bash
cd task_0001/output
npm install
npm run dev
```

访问 `http://localhost:5173` 查看还原的网页。

## 核心功能说明

### 1. Vue3多文件提取器 (`backend/codegen/vue_extractor.py`)

从AI响应中提取多个文件:

```python
from codegen.vue_extractor import parse_vue_project_structure

ai_response = """
<file path="index.html">...</file>
<file path="src/App.vue">...</file>
"""

files = parse_vue_project_structure(ai_response)
# Returns: {'index.html': '...', 'src/App.vue': '...', ...}
```

### 2. 批量处理API (`backend/routes/batch_process.py`)

主要端点:

- `GET /api/tasks` - 列出所有task目录
- `POST /api/batch-process` - 处理单个task

处理流程:

1. 读取最后一张截图 (`checkpoints/step_XX.png`)
2. 转换为base64并构建Prompt
3. 调用OpenAI GPT-4V生成Vue3代码
4. 解析多文件输出
5. 复制assets资源文件
6. 写入output目录

### 3. Vue3 Prompt模板

专门设计的Prompt要求AI生成Vue3+Vite项目:

- 使用 `<file path="...">` 格式输出多文件
- 包含完整的配置文件 (package.json, vite.config.ts等)
- 使用Vue 3 Composition API + TypeScript
- 支持组件拆分 (src/components/*.vue)

## 配置说明

### 环境变量

```bash
# 必需
OPENAI_API_KEY=sk-xxx

# 可选
IS_DEBUG_ENABLED=true
DEBUG_DIR=./debug
```

### 修改模型

在 `batch_process.py` 中修改:

```python
await call_openai(
    messages=prompt_messages,
    api_key=api_key,
    model="gpt-4-vision-preview"  # 修改这里
)
```

支持的模型:
- `gpt-4-vision-preview` (推荐)
- `gpt-4o`
- `gpt-4-turbo`

## 开发指南

### 添加新的Stack类型

1. 修改 `backend/prompts/prompt_types.py`:

```python
Stack = Literal[
    # ...existing stacks
    "vue3_vite",
]
```

2. 修改 `frontend/src/lib/stacks.ts`:

```typescript
export enum Stack {
  // ...existing stacks
  VUE3_VITE = "vue3_vite",
}
```

### 自定义Prompt模板

修改 `backend/routes/batch_process.py` 中的 `build_vue3_prompt()` 函数。

### 扩展文件提取器

修改 `backend/codegen/vue_extractor.py` 以支持更多文件格式。

## 测试

### 单元测试

```bash
cd screenshot-to-code/backend
pytest tests/
```

### 集成测试

```bash
python test_batch_process.py
```

## 常见问题

### Q: OpenAI API调用失败

A: 检查:
1. API Key是否正确
2. 是否有GPT-4 Vision权限
3. 账户余额是否充足

### Q: 生成的项目无法运行

A: 确保:
1. 已执行 `npm install`
2. Node.js版本 >= 18
3. 检查控制台错误信息

### Q: 如何对比生成结果与ground_truth

A: 
1. 查看 `task_0001/ground_truth/` 目录
2. 对比生成的 `task_0001/output/` 目录
3. 可以手动对比或使用diff工具

## 技术栈

- **后端**: Python 3.10+, FastAPI, OpenAI API
- **前端**: React, TypeScript, TailwindCSS (保留原项目)
- **生成目标**: Vue 3, Vite, TypeScript
- **AI模型**: GPT-4 Vision

## 许可证

基于原项目 [screenshot-to-code](https://github.com/abi/screenshot-to-code) 的AGPL-3.0许可证。

## 贡献

欢迎提交Issue和Pull Request!

## 联系方式

如有问题,请提交Issue或联系开发者。
