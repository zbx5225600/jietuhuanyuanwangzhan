# 更新说明 - 配置文件支持

## 主要更新

### ✨ 新增功能

1. **配置文件支持**
   - 支持通过 `config.json` 配置API Key
   - 无需每次设置环境变量
   - 配置持久化，重启终端后仍有效

2. **配置向导**
   - 新增 `setup_config.py` 交互式配置工具
   - 自动验证API Key格式
   - 支持选择AI模型

3. **配置优先级**
   - config.json > 环境变量
   - 灵活切换不同配置

4. **安全保护**
   - 自动添加 `.gitignore`
   - 防止API Key泄露到Git仓库

## 新增文件

### 配置相关
- `config.json` - 实际配置文件 (用户创建，不提交Git)
- `config.example.json` - 配置模板
- `setup_config.py` - 配置向导脚本
- `setup_config.bat` - Windows配置向导快捷方式
- `.gitignore` - Git忽略规则

### 文档
- `配置说明.md` - 详细配置文档
- `配置方式总结.md` - 三种配置方式对比
- `使用流程.md` - 完整使用流程图
- `更新说明.md` - 本文档

## 修改的文件

### 脚本更新
- `process_single.py` - 支持从config.json读取配置
- `process_task.py` - 支持从config.json读取配置
- `check_env.py` - 检查config.json和环境变量

### 文档更新
- `README_NEW.md` - 添加配置向导说明
- `快速开始.md` - 更新配置步骤
- `使用说明.md` - 添加配置说明链接

## 使用方式对比

### 之前 (仅环境变量)

```bash
# 每次都要设置
$env:OPENAI_API_KEY="sk-xxx"
python process_single.py task_0001

# 重启终端后失效，需要重新设置
$env:OPENAI_API_KEY="sk-xxx"
python process_single.py task_0002
```

### 现在 (配置文件)

```bash
# 首次配置一次
python setup_config.py

# 之后直接使用，无需重复设置
python process_single.py task_0001
python process_single.py task_0002
python process_single.py task_0003
```

## 配置方式

### 方式1: 配置向导 (推荐) ⭐

```bash
python setup_config.py
```

交互式配置，最简单！

### 方式2: 手动配置文件

```bash
copy config.example.json config.json
notepad config.json
```

### 方式3: 环境变量 (仍然支持)

```bash
$env:OPENAI_API_KEY="sk-xxx"
```

## 配置优先级

```
config.json (优先) > 环境变量 (备用)
```

如果 `config.json` 存在且有效，会优先使用配置文件中的API Key。

## 迁移指南

### 从环境变量迁移到配置文件

```bash
# 1. 运行配置向导
python setup_config.py

# 2. 输入之前的API Key
# 3. 完成！之后无需设置环境变量
```

### 保留环境变量方式

如果你更喜欢环境变量，完全可以继续使用：

```bash
$env:OPENAI_API_KEY="sk-xxx"
python process_single.py task_0001
```

两种方式可以共存，配置文件优先。

## 安全改进

### 新增 .gitignore

```gitignore
# API Keys和敏感配置
config.json
.env
.env.local
```

### 配置文件保护

- `config.json` - 不提交到Git (包含真实API Key)
- `config.example.json` - 提交到Git (仅作为模板)

### 最佳实践

1. ✅ 使用 `config.json` 存储API Key
2. ✅ 不要提交 `config.json` 到Git
3. ✅ 使用 `config.example.json` 作为模板
4. ✅ 定期轮换API Key

## 配置文件格式

### 最小配置

```json
{
  "openai_api_key": "sk-your-key-here"
}
```

### 完整配置

```json
{
  "openai_api_key": "sk-your-key-here",
  "model": "gpt-4-vision-preview",
  "max_tokens": 4096,
  "temperature": 0.2,
  "openai_base_url": "",
  "debug_enabled": false
}
```

## 快速开始

### 新用户

```bash
# 1. 安装依赖
pip install fastapi uvicorn openai python-dotenv aiofiles

# 2. 配置API Key
python setup_config.py

# 3. 检查环境
python check_env.py

# 4. 处理task
python process_single.py task_0001
```

### 现有用户

如果你已经在使用环境变量：

```bash
# 可选: 迁移到配置文件
python setup_config.py

# 或继续使用环境变量
$env:OPENAI_API_KEY="sk-xxx"
python process_single.py task_0001
```

## 常见问题

### Q: 必须使用配置文件吗？

A: 不是必须的。环境变量方式仍然支持，两种方式可以共存。

### Q: 配置文件在哪里？

A: 项目根目录的 `config.json`

### Q: 如何修改配置？

A: 
```bash
# 方式1: 重新运行配置向导
python setup_config.py

# 方式2: 直接编辑
notepad config.json
```

### Q: 配置文件会被提交到Git吗？

A: 不会。已在 `.gitignore` 中排除。

### Q: 两种方式都设置了，用哪个？

A: 优先使用 `config.json` 中的配置。

## 验证配置

```bash
python check_env.py
```

输出示例：

```
API Key检查:
  ✅ config.json中已设置: sk-proj-xx...
```

## 相关文档

- [配置说明.md](配置说明.md) - 详细配置文档
- [配置方式总结.md](配置方式总结.md) - 三种方式对比
- [使用流程.md](使用流程.md) - 完整流程图
- [快速开始.md](快速开始.md) - 快速上手

## 总结

这次更新主要解决了"每次都要设置环境变量"的问题，通过配置文件实现了：

✅ 配置持久化  
✅ 一次配置，永久使用  
✅ 交互式配置向导  
✅ 安全的Git忽略规则  
✅ 向后兼容环境变量方式  

推荐使用配置文件方式，更方便！
