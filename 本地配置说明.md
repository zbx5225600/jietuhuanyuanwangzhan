# 本地配置说明 - config.json

## 概述

项目**已经支持**本地配置文件自动获取。运行主程序 (`process_single.py`) 时，会自动从项目根目录的 `config.json` 读取配置。

## 配置文件位置

```
jietuhuanyuanwangzhan/
├── config.json          ← 在这里
├── process_single.py     ← 主程序自动读取这里
├── setup_config_multi.py
└── ...
```

## 完整配置示例

### 豆包 Coding Plan 配置（推荐你的使用场景）

```json
{
  "provider": "doubao",
  "model": "doubao-seed-2.0-code",
  "max_tokens": 4096,
  "temperature": 0.2,
  "doubao_api_key": "ark-你的APIKey在这里",
  "doubao_base_url": "https://ark.cn-beijing.volces.com/api/coding/v3"
}
```

### 其他模型配置示例

#### OpenAI GPT-4V
```json
{
  "provider": "openai",
  "model": "gpt-4-vision-preview",
  "max_tokens": 4096,
  "temperature": 0.2,
  "openai_api_key": "sk-你的APIKey在这里",
  "openai_base_url": "https://api.openai.com/v1"
}
```

#### 通义千问 Qwen-VL
```json
{
  "provider": "qwen",
  "model": "qwen-vl-max",
  "max_tokens": 4096,
  "temperature": 0.2,
  "qwen_api_key": "你的APIKey在这里"
}
```

#### 智谱GLM
```json
{
  "provider": "glm",
  "model": "glm-4.1v-thinking-flash",
  "max_tokens": 8192,
  "temperature": 0.2,
  "glm_api_key": "你的APIKey在这里"
}
```

#### 文心一言 ERNIE
```json
{
  "provider": "ernie",
  "model": "ernie-bot-4",
  "max_tokens": 4096,
  "temperature": 0.2,
  "ernie_api_key": "你的APIKey在这里",
  "ernie_secret_key": "你的SecretKey在这里"
}
```

#### 豆包独立视觉模型（按量付费）
```json
{
  "provider": "doubao",
  "model": "doubao-vision-pro",
  "max_tokens": 4096,
  "temperature": 0.2,
  "doubao_api_key": "ark-你的APIKey在这里"
}
```

## 配置字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `provider` | ✅ | AI提供商: `openai`\|`qwen`\|`ernie`\|`glm`\|`doubao` |
| `model` | ✅ | 模型名称，见下文 |
| `max_tokens` | ☐ | 最大输出tokens，默认 `4096` |
| `temperature` | ☐ | 温度，默认 `0.2`（较低更稳定） |
| `openai_api_key` | provider=openai时必填 | OpenAI API Key |
| `openai_base_url` | ☐ | 自定义OpenAI API地址 |
| `qwen_api_key` | provider=qwen时必填 | 通义千问API Key |
| `ernie_api_key` | provider=ernie时必填 | 文心一言API Key |
| `ernie_secret_key` | provider=ernie时必填 | 文心一言Secret Key |
| `glm_api_key` | provider=glm时必填 | 智谱GLM API Key |
| `doubao_api_key` | provider=doubao时必填 | 豆包API Key（格式：`ark-xxx`） |
| `doubao_base_url` | ☐ | 豆包自定义Base URL，**Coding Plan必须填** |

## 支持的模型列表

### 豆包 (provider: doubao)

| 模型 | 说明 | 适用场景 |
|------|------|----------|
| `doubao-seed-2.0-code` | ⭐ **推荐** Coding Plan 订阅包含，编程优化，支持视觉理解 | 你的使用场景 - 视频抽帧后处理 |
| `doubao-seed-2.0-pro` | Coding Plan 订阅包含，全能模型，支持视觉 | Coding Plan 用户备选 |
| `doubao-vision-pro` | 独立视觉模型，按量付费，最强视觉 | 需要更好视觉质量 |
| `doubao-vision-lite` | 独立视觉模型，按量付费，轻量版 | 追求性价比 |
| `doubao-4-vision` | 独立视觉模型，按量付费，旗舰版 | 追求极致质量 |

### OpenAI (provider: openai)

| 模型 | 说明 |
|------|------|
| `gpt-4-vision-preview` | 推荐，最准确 |
| `gpt-4o` | 更快更便宜 |
| `gpt-4-turbo` | 平衡 |

### 通义千问 (provider: qwen)

| 模型 | 说明 |
|------|------|
| `qwen-vl-max` | 推荐，最强视觉理解 |
| `qwen-vl-plus` | 平衡性能和成本 |

### 智谱GLM (provider: glm)

| 模型 | 说明 |
|------|------|
| `glm-4.1v-thinking-flash` | 推荐，最新思维链模型，性价比极高 |
| `glm-4v-plus` | 增强版视觉 |
| `glm-4v` | 经典视觉 |
| `glm-4` | 纯文本 |

### 文心一言 (provider: ernie)

| 模型 | 说明 |
|------|------|
| `ernie-bot-4` | 推荐 |
| `ernie-bot-turbo` | 更快 |

## 🔥 Coding Plan 用户重要提示

如果你使用 **Doubao-Seed-2.0-Code + Coding Plan**，`doubao_base_url` **必须**设置为：

```
"doubao_base_url": "https://ark.cn-beijing.volces.com/api/coding/v3"
```

不填这个地址会调用失败！

## 使用方式

### 方式一：交互式配置（推荐新手）

```bash
python setup_config_multi.py
```

按照提示一步步选择，会自动生成 `config.json`。

### 方式二：手动编辑

直接复制上面的配置示例，修改为你自己的API Key，保存为 `config.json` 放在项目根目录。

### 运行处理

配置好后直接运行：

```bash
# 处理单个task
python process_single.py task_0001
```

程序会**自动读取 `config.json`**，不需要你每次输入配置。

## 环境变量回退

如果 `config.json` 中没有找到API Key，程序会自动尝试从环境变量读取：

| 提供商 | 环境变量名称 |
|--------|--------------|
| openai | `OPENAI_API_KEY` |
| qwen | `QWEN_API_KEY` |
| ernie | `ERNIE_API_KEY` |
| glm | `GLM_API_KEY` |
| doubao | `DOUBAO_API_KEY` |

所以你也可以不写在 `config.json` 里，完全通过环境变量配置。

## 常见问题

### Q: config.json 需要提交到git吗？

A: 不需要，`.gitignore` 已经包含了 `config.json`，你的API Key不会被提交。

### Q: 如何切换模型？

A: 直接编辑 `config.json`，修改 `provider` 和 `model` 和对应的API Key即可。

### Q: 可以同时配置多个模型吗？

A: 可以，在 `config.json` 里把所有API Key都填上，只用修改 `provider` 来切换。

示例：
```json
{
  "provider": "doubao",
  "model": "doubao-seed-2.0-code",
  "doubao_api_key": "xxx",
  "doubao_base_url": "https://ark.cn-beijing.volces.com/api/coding/v3",
  "glm_api_key": "yyy",
  "qwen_api_key": "zzz"
}
```

切换只需要改 `provider` 这一行：
```json
"provider": "glm"
```

### Q: 配置完了怎么验证？

A: 直接运行：
```bash
python process_single.py task_0001
```

如果没有报错开始处理，就是配置对了。

## 你的配置（Doubao-Seed-2.0-Code + Coding Plan）

直接复制这个模板，替换API Key即可：

```json
{
  "provider": "doubao",
  "model": "doubao-seed-2.0-code",
  "max_tokens": 4096,
  "temperature": 0.2,
  "doubao_api_key": "替换为你的Coding Plan API Key",
  "doubao_base_url": "https://ark.cn-beijing.volces.com/api/coding/v3"
}
```

保存为 `config.json` 放在项目根目录，然后运行：

```bash
python process_single.py 你的task目录名
```

就这么简单！
