# 配置说明

## API Key配置方式

本项目支持两种方式配置OpenAI API Key：

### 方式1: 配置文件 (推荐) ⭐

在项目根目录创建或编辑 `config.json` 文件：

```json
{
  "openai_api_key": "sk-your-actual-api-key-here",
  "model": "gpt-4-vision-preview",
  "max_tokens": 4096,
  "temperature": 0.2
}
```

**优点:**
- ✅ 无需每次设置环境变量
- ✅ 配置持久化，重启终端后仍有效
- ✅ 可以同时配置其他参数
- ✅ 适合开发环境

**步骤:**

1. 复制示例配置文件：
```bash
# Windows
copy config.example.json config.json

# Linux/Mac
cp config.example.json config.json
```

2. 编辑 `config.json`，填入你的API Key：
```json
{
  "openai_api_key": "sk-proj-xxxxxxxxxxxxx"
}
```

3. 直接运行脚本：
```bash
python process_single.py task_0001
```

### 方式2: 环境变量

临时设置（当前会话有效）：

```bash
# Windows PowerShell
$env:OPENAI_API_KEY="sk-your-api-key-here"

# Windows CMD
set OPENAI_API_KEY=sk-your-api-key-here

# Linux/Mac
export OPENAI_API_KEY="sk-your-api-key-here"
```

永久设置：

**Windows:**
1. 右键"此电脑" → 属性 → 高级系统设置
2. 环境变量 → 新建用户变量
3. 变量名: `OPENAI_API_KEY`
4. 变量值: `sk-your-api-key-here`

**Linux/Mac:**
编辑 `~/.bashrc` 或 `~/.zshrc`：
```bash
echo 'export OPENAI_API_KEY="sk-your-api-key-here"' >> ~/.bashrc
source ~/.bashrc
```

## 配置优先级

当两种方式都设置时，优先级为：

```
config.json > 环境变量
```

即：如果 `config.json` 中有配置，会优先使用配置文件中的值。

## 完整配置选项

`config.json` 支持以下配置项：

```json
{
  "openai_api_key": "sk-your-api-key-here",
  "model": "gpt-4-vision-preview",
  "max_tokens": 4096,
  "temperature": 0.2,
  "openai_base_url": "",
  "debug_enabled": false
}
```

### 配置项说明

| 配置项 | 说明 | 默认值 | 可选值 |
|--------|------|--------|--------|
| `openai_api_key` | OpenAI API密钥 | 必填 | sk-xxx |
| `model` | 使用的AI模型 | gpt-4-vision-preview | gpt-4o, gpt-4-turbo |
| `max_tokens` | 最大生成token数 | 4096 | 1000-16000 |
| `temperature` | 生成随机性 | 0.2 | 0.0-2.0 |
| `openai_base_url` | 自定义API地址 | 空 | https://... |
| `debug_enabled` | 调试模式 | false | true/false |

## 模型选择

### gpt-4-vision-preview (推荐)
- 最准确的视觉理解
- 生成质量最高
- 速度较慢
- 成本较高 (~$0.01-0.05/次)

### gpt-4o
- 速度更快
- 成本较低
- 质量略低于vision-preview

### gpt-4-turbo
- 平衡速度和质量
- 中等成本

## 安全建议

### ⚠️ 重要提示

1. **不要提交API Key到Git仓库**
   
   已在 `.gitignore` 中添加：
   ```
   config.json
   .env
   ```

2. **使用示例配置文件**
   
   提供 `config.example.json` 作为模板，不包含真实API Key

3. **定期轮换API Key**
   
   建议定期在OpenAI后台更换API Key

4. **限制API Key权限**
   
   在OpenAI后台设置使用限额和权限

## 检查配置

运行环境检查脚本：

```bash
python check_env.py
```

输出示例：

```
API Key检查:
  ✅ config.json中已设置: sk-proj-xx...
```

## 常见问题

### Q: config.json不存在怎么办？

A: 复制示例文件：
```bash
copy config.example.json config.json
```

### Q: 两种方式都设置了，用哪个？

A: 优先使用 `config.json` 中的配置

### Q: 如何临时使用不同的API Key？

A: 使用环境变量临时覆盖：
```bash
$env:OPENAI_API_KEY="sk-temp-key"
python process_single.py task_0001
```

### Q: 配置文件可以放在其他位置吗？

A: 目前只支持项目根目录的 `config.json`

### Q: 如何验证API Key是否有效？

A: 运行一次处理脚本，如果API Key无效会立即报错

## 示例

### 完整配置示例

```json
{
  "openai_api_key": "sk-proj-abcdefghijklmnopqrstuvwxyz1234567890",
  "model": "gpt-4-vision-preview",
  "max_tokens": 4096,
  "temperature": 0.2,
  "openai_base_url": "",
  "debug_enabled": false
}
```

### 最小配置示例

```json
{
  "openai_api_key": "sk-proj-your-key-here"
}
```

其他配置项会使用默认值。

## 获取API Key

1. 访问 [OpenAI Platform](https://platform.openai.com/)
2. 登录账号
3. 进入 API Keys 页面
4. 点击 "Create new secret key"
5. 复制生成的API Key (sk-proj-xxx)
6. 粘贴到 `config.json` 中

## 下一步

配置完成后，运行：

```bash
# 检查配置
python check_env.py

# 处理task
python process_single.py task_0001
```
